# NOTE: This script has been automatically generated by github.com/ericdill/conda-skeletor

package:
  name: filestore
  version: 0.2.1

source:
  git_url: https://github.com/NSLS-II/filestore
  git_rev: v0.2.1
  patches:
    - config.patch
build:
  number: 0

  # TODO add entry points

requirements:
  build:
    - python
  run:
    - python
    - boltons # When they're not builtins, they're boltons. (pypi)
    - h5py # Read and write HDF5 files from Python. (conda)
    - jsonschema # An implementation of JSON Schema validation for Python (conda)
    - mongoengine # MongoEngine is a Python Object-Document Mapper for working with MongoDB. (pypi)
    - numpy # array processing for numbers, strings, records, and objects. (conda)
    - pims # Python Image Sequence (pypi)
    - pyyaml # YAML parser and emitter for Python (conda)
    - six # Python 2 and 3 compatibility utilities (conda)
    - tifffile # Read and write image data from and to TIFF files. (pypi)

test:
  requires:
    - nose # nose extends unittest to make testing easier (conda)
    - nslsii_dev_configuration
  imports:
    - filestore
    - filestore.api
    - filestore.commands
    - filestore.conf
    - filestore.file_writers
    - filestore.handlers
    - filestore.odm_templates
    - filestore.path_only_handlers
    - filestore.readers
    - filestore.readers.spe
    - filestore.retrieve
    - filestore.utils

about:
  home: https://github.com/NSLS-II/filestore
  license: BSD (3-clause)


# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
# and http://github.com/ericdill/conda-skeletor/readme.rst for more
# information about how this recipe was generated

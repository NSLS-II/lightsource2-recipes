package:
  name: boltons
  version: {{ environ.get('GIT_DESCRIBE_TAG', 'GIT_STUB') }}.post{{ environ.get('GIT_DESCRIBE_NUMBER', 'GIT_STUB') }}

source:
  git_url: https://github.com/mahmoud/boltons.git
  git_rev: master

build:
  number: 1
  string: {{ environ.get('GIT_BUILD_STR', 'GIT_STUB') }}_py{{ py }}


requirements:
  build:
    - python
    - pytest

  run:
    - python

test:
  imports:
    - boltons

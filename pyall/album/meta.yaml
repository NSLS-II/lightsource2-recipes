package:
  name: album
  version: {{ GIT_DESCRIBE_TAG }}

source:
  git_url: https://github.com/NSLS-II/album
  git_rev: master

build:
  number: 1
  entry_points:
    - album = album.server:run

requirements:
  build:
    - python
    - numpy
    - setuptools

  run:
    - python
    - bokeh
    - flask
    - databroker >=v0.3.0
    - numpy

test:
  imports:
    - album

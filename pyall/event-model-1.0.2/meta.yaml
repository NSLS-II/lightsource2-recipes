package:
  name: event-model
  version: 1.0.2

source:
  git_url: https://github.com/NSLS-II/event-model
  git_rev: v1.0.2

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - enum34 [py2k]

test:
  imports:
    - event_model

about:
  home: https://github.com/NSLS-II/event_model
  license: BSD

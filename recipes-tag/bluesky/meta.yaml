{% set version = "0.10.0rc1" %}

package:
    name: bluesky
    version: {{ version }}

source:
    git_url: https://github.com/nsls-ii/bluesky
    git_rev: v{{ version }}

build:
    number: 0
    script: python setup.py install --single-version-externally-managed --record=record.txt
    skip: True  # [py2k]

requirements:
    build:
        - python

    run:
        - boltons
        - cycler
        - databroker
        - doct
        - event-model
        - historydict
        - jinja2
        - jsonschema
        - matplotlib
        - numpy
        - python
        - scipy
        - super_state_machine
        - tqdm
        - tifffile
        - traitlets
        - xray-vision

test:
    imports:
        - bluesky

about:
    home: https://github.com/NSLS-II/bluesky
    license: BSD

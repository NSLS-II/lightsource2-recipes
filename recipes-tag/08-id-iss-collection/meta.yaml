{% set year = "2019" %}
{% set cycle = "3" %}
{% set version = "0" %}

package:
  name: 08-id-iss-collection
  version: {{ year }}C{{ cycle }}.{{ version }}

build:
  number: 0

requirements:
  run:
    - 08-id-iss-analysis
    - pexpect
    - swift-t
